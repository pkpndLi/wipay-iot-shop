<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".LoadConfigActivity">

    <LinearLayout
        android:layout_width="357dp"
        android:layout_height="512dp"
        android:orientation="vertical"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <Button
            android:id="@+id/btn_addaid"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="add aid" />

        <Button
            android:id="@+id/btn_addcapk"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="add capk" />

        <Button
            android:id="@+id/btn_addterm"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="add term" />

        <Button
            android:id="@+id/btn_clearaid"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="clear all aid" />

        <Button
            android:id="@+id/btn_clearcapk"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="clear all capk" />

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="214dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tv_loadconfig"
                    android:layout_width="match_parent"
                    android:layout_height="669dp"
                    android:text="TextView" />
            </LinearLayout>
        </ScrollView>

        <Button
            android:id="@+id/btn_loadconfig"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="load config" />

    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>